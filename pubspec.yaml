name: flutter_tag_locator
description: A tool to find the oldest Flutter release tag for a given commit.
publish_to: none
environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.4.0
  http: ^1.1.0
  path: ^1.8.0
  pub_semver: ^2.1.4

dev_dependencies:
  dart_flutter_team_lints: ^3.5.2
